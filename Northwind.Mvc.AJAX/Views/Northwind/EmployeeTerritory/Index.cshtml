@model EmployeeTerritoryCollectionModel

<div id="Ajax">
</div>

<script>
    $(function () {
        try {
            $("#Ajax").load("@(Url.Action("Search", "EmployeeTerritory", null, Request.Url.Scheme))");
        } catch (exception) {
            zExceptionMessage("EmployeeTerritory Index.cshtml", "function", exception.message)
        }
    });
</script>