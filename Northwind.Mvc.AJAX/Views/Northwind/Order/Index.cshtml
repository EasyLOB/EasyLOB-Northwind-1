@model OrderCollectionModel

<div id="Ajax">
</div>

<script>
    $(function () {
        try {
            $("#Ajax").load("@(Url.Action("Search", "Order", null, Request.Url.Scheme))");
        } catch (exception) {
            zExceptionMessage("Order Index.cshtml", "function", exception.message)
        }
    });
</script>