@model OrderDetailCollectionModel

<div id="Ajax">
</div>

<script>
    $(function () {
        try {
            $("#Ajax").load("@(Url.Action("Search", "OrderDetail", null, Request.Url.Scheme))");
        } catch (exception) {
            zExceptionMessage("OrderDetail Index.cshtml", "function", exception.message)
        }
    });
</script>