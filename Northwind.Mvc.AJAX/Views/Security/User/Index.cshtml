@model UserCollectionModel

<div id="Ajax">
</div>

<script>
    $(function () {
        try {
            $("#Ajax").load("@(Url.Action("Search", "User", null, Request.Url.Scheme))");
        } catch (exception) {
            zExceptionMessage("User Index.cshtml", "function", exception.message)
        }
    });
</script>