@model UserClaimCollectionModel

<div id="Ajax">
</div>

<script>
    $(function () {
        try {
            $("#Ajax").load("@(Url.Action("Search", "UserClaim", null, Request.Url.Scheme))");
        } catch (exception) {
            zExceptionMessage("UserClaim Index.cshtml", "function()", exception.message)
        }
    });
</script>